# 検索機能の挙動設計

## 現在のUIフロー分析

### 1. HOMEページからの検索 ✅
- テキスト入力 → Enter/Searchボタン → SearchPageに遷移して検索実行
- Advanced Searchボタン → SearchPageに遷移（検索なし）
- **現状で問題なし**

### 2. Advanced Searchページでの検索挙動 🤔
**現在の実装:**
- SearchBar: テキスト検索
- AdvancedFilters: 詳細フィルター（Show/Hide切り替え）
- フィルター変更時: 自動的に再検索実行
- Apply Filtersボタン: フィルターのみで検索

**検討ポイント:**
- リアルタイム検索 vs 明示的な検索実行どちらが良い？
- フィルター変更の度に自動再検索するのは適切？
- ユーザビリティとパフォーマンスのバランス

### 3. 検索結果・履歴の扱い 🤔
**現在の実装:**
- 空クエリ + フィルターのみでも検索可能
- 検索履歴は10件まで保存
- 結果がない場合の処理

**検討ポイント:**
- 空クエリ + フィルターのみ検索の意味づけ
- 検索履歴にフィルター情報も含めるべき？
- 「フィルターのみ検索」と「テキスト+フィルター検索」の区別

## 改善方針決定

### ✅ 検索実行タイミング: **手動実行**
- 明示的なSearchボタンでのみ検索実行
- フィルター変更時の自動再検索を廃止
- ユーザーが意図した時のみ検索が実行される

### ✅ 名称変更: **Advanced Search Options**  
- "Advanced Filters" → "Advanced Search Options"
- より適切な表現に変更

### ✅ 空クエリ+Advanced Search Options検索: **有効**
- テキストなし + フィルターのみでの検索を許可
- 例：「2023年の写真で、ISO 800以上」のような条件検索
- パフォーマンス対策：`Max Photos Per Fetch`設定を上限として適用

### ✅ 検索結果のソート機能: **現在未実装**
**重要な発見**: 現在の`search_photos`関数には**ORDER BY句が一切ない**
- 検索結果は挿入順序（ランダム）で返される
- 100件で切り捨てているが、どの100件かは不定

**必要なソート方法（優先度順）:**
1. **撮影日時順** (新しい順/古い順) - 最も重要
2. **星評価順** (高い順) - 品質重視
3. **追加日順** (最近追加分優先) - インポート管理に有用
4. **ファイル名順** (アルファベット順) - あまり重要でない

**実装方針:**
- フロントエンドからソート指定を受け取る
- SQLクエリにORDER BY句を追加
- デフォルトは「撮影日時新しい順」

### ✅ 検索履歴の改善方針
1. **Advanced Search Optionsも履歴に含める**
   - フィルター条件も保存して再利用可能に
2. **履歴の表示改善**
   - モーダルで詳細表示の検討
3. **無効な検索の扱い**
   - 空クエリ+フィルターなしの場合は検索自体を実行しない

## 実装変更点
1. SearchTools.jsx: フィルター変更時の自動再検索を削除
2. AdvancedFilters → AdvancedSearchOptions へリネーム
3. Apply Filtersボタンの扱い見直し
4. 検索履歴にフィルター情報追加
5. 空クエリ+フィルターなしの検索実行を防止
6. Max Photos Per Fetch設定を検索結果上限として適用
7. **【重要】検索結果ソート機能の追加**
   - Rustバックエンド: search_photosにソートパラメータ追加
   - SQLクエリにORDER BY句追加
   - フロントエンド: ソート選択UI追加