[package]
name = "video-server"
version = "0.0.1"
edition = "2021"

[dependencies]
warp = "0.3"
tokio = { version = "1.0", features = ["rt-multi-thread", "macros", "fs", "io-util", "sync", "time", "net"] } 
tokio-util = { version = "0.7", features = ["io"] }
futures-util = "0.3"
base64 = "0.21"
log = "0.4"
serde_json = "1.0"
