# Remove useFetchConfig hook from App.jsx

## Task Description
Remove the useFetchConfig hook import and usage from App.jsx as part of fetchConfig deprecation.

## Implementation Details

### Current Code to Remove
```javascript
// Import to remove
import { useFetchConfig } from '../hooks/useFetchConfig.js';

// Hook usage to remove
const fetchConfig = useFetchConfig();
```

### Changes Required

#### 1. Remove Import Statement
```javascript
// Remove this line
import { useFetchConfig } from '../hooks/useFetchConfig.js';
```

#### 2. Remove Hook Usage
```javascript
// Remove this line
const fetchConfig = useFetchConfig();
```

#### 3. Clean Up Related Logic
Remove any conditional logic that depends on fetchConfig:
```javascript
// Remove patterns like:
if (!fetchConfig) return <Loading />;
if (fetchConfig.some_property) { ... }
```

## Files to Modify
- `src/App.jsx` - Remove useFetchConfig import and usage

## Success Criteria
- [ ] useFetchConfig import statement removed
- [ ] fetchConfig variable assignment removed
- [ ] All fetchConfig-dependent logic removed or replaced
- [ ] App.jsx compiles without errors
- [ ] Application functions correctly without fetchConfig

## Dependencies
- 78-5: fetchConfig dependency must be removed from PhotosList.jsx first
- 78-7: fetchConfig props must be removed from PhotosList component

## Related Tasks
- 78-7: Remove fetchConfig props from PhotosList component in App.jsx
- 78-8: Delete useFetchConfig.js file (113 lines)

## Order of Operations
1. Complete tasks 78-1 through 78-5 first
2. Remove fetchConfig props from PhotosList (78-7)
3. Remove useFetchConfig hook usage (this task)
4. Delete useFetchConfig.js file (78-8)

## Testing
- Verify App.jsx compiles successfully
- Test that all view modes continue to work
- Ensure no runtime errors related to missing fetchConfig