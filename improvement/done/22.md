# Fix thumbnail image URL

getThumbnailSrc returns thumbnail path from original path.
But it assumes original path is like /path/to/$date/image.jpg.
note that: $date is yyyy-mm-dd like '2025-07-01'
Currently, uuid directory exists after /path/to/$date/.
Please fix it.
