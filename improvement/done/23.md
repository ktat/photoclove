# Add google photo URL to `photo_metadata`

At first, add column `google_photos_url` to `photo_metadata`.
This app has the feature to upload phtos to Google Photos using API.
Currently using batchCreate API, upload files.
Use its response and update `photo_metadata`.
