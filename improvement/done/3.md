# Use SQLite instead of TSV

Currently, photo data is stored into tsv file under the date directories.
It's slow to read all files.

Please use one SQLite DB instead of TSV files.
TSV implementation is implemented as interface.
So, you should implement SQLite implementation using same interface.
