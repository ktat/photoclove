# Improve LogViewer

- Allow to delete background log like frontend log.
- Export Log works?
   - assumed behavior
       1. click `Export Log` 
       2. file is created and download to download directory which is spcified in Preferences
       3. exported content is filtered content

