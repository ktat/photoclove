# Add filter

create new extension filter to src/App/PhotosList.jsx.
When fetching list of photos, fetch photos whose extension is selected filter.

extensions for filter(case insensitive)
- jpeg(include jpeg)
- mp4
- gif
- etc. (any extentions which photo manager hancldes)

