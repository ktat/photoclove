# Delete file from DB

When photo is deleted with 'del' key in one date,
the photo is removed from directory, but currently not deleted from DB.
Fix it.

And if `(re)Create database of the date` is invoked, 
also delete file from DB if the file is not in the date directory.
