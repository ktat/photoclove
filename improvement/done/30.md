# Fix minor bug when loading date

When a user select one date from `List of Date`,
Loading image is appeared.
While loading image is appeared, a user select another date
and then unconsistet status selected date and list of photos.

To avoid this bug:

When a user selects another date while photo loading,
stop current loading and start photo loading with new user selected date.
