# Remove fetchConfig props from PhotosList component in App.jsx

## Task Description
Remove the fetchConfig prop from the PhotosList component usage in App.jsx.

## Implementation Details

### Current Code Pattern
```javascript
<PhotosList
    fetchConfig={fetchConfig}
    config={config}
    viewMode={viewMode}
    // ...other props
/>
```

### New Code Pattern
```javascript
<PhotosList
    config={config}
    viewMode={viewMode}
    // ...other props
    // fetchConfig prop removed
/>
```

## Files to Modify
- `src/App.jsx` - Remove fetchConfig prop from PhotosList component

## Success Criteria
- [ ] fetchConfig prop removed from PhotosList component
- [ ] All other props remain intact
- [ ] Component renders without errors
- [ ] PhotosList functions correctly without fetchConfig prop

## Dependencies
- 78-5: fetchConfig dependency must be removed from PhotosList.jsx first
- PhotosList component must be updated to not expect fetchConfig prop

## Related Tasks
- 78-5: Remove fetchConfig dependency from PhotosList.jsx
- 78-6: Remove useFetchConfig hook from App.jsx

## Order of Operations
1. Complete PhotosList.jsx updates (78-1 through 78-5)
2. Remove fetchConfig prop from component usage (this task)
3. Remove useFetchConfig hook usage (78-6)
4. Delete useFetchConfig.js file (78-8)

## Testing
- Verify PhotosList component renders correctly
- Test all view modes work without fetchConfig prop
- Ensure no prop validation warnings or errors