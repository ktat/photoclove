# remove filter tab

Curretly Filter tab exists.
It is realtime filter for list of photos(result).
It is confusing with AdvancedSearch.

So remove from tab and put icon which represents `filter` to top right of PhotosList.
Click it and open small modal below the icon and User can change filter and
filters are immediately reflected.
