# Improve UI for toggle PhotoOption.jsx

currently '<' is clicked PhotoOption.jsx content is appered(to be wider).
and '>' is clicked PhotoOption.jsx content is hide(to be narrower).

## Improvement

- instead of '<', show `Info` and `Editor` as like vertical tab.
- If user click `Info`, show content of PhotoOption.jsx and `Info` tab is selected.
- If user click `Editor`, show content of PhotoOption.jsx and `Editor` tab is selected.

## UI Adjustment

Table in Photo Info  is overflowed.
it should be a bit narrower to be within rith column.
