# Fix layout of import page

Import page design is not good.
`Select All photos in this page`, `Select All photos in all pages` and `Unselect All` buttons' margin top is too wider. maybe, importer-files-list grid(?) margin is too wide.
