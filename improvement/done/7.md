# Fix zoom

Your changes for zoom is worse than before implementation.
Zoom feature specification:

1. zoom with ctrl + mouse wheel 
2. don't change coodinate of image. for example, 100px x 100px image, if mouse curosr is x:50px, y:50px, don't change coordinate while zooming. zoom in/out from cursor position as center.
